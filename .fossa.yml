version: 3

project:
  locator: SolaceProducts_solace-spring-cloud
  id: SolaceProducts_solace-spring-cloud
  name: solace-spring-cloud
  teams: []
  labels:
    - java

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
